"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MESH_API_CONTEXT_SYMBOL = void 0;
exports.getInContextSDK = getInContextSDK;
const graphql_1 = require("graphql");
const batch_delegate_1 = require("@graphql-tools/batch-delegate");
const delegate_1 = require("@graphql-tools/delegate");
const utils_1 = require("@graphql-tools/utils");
const wrap_1 = require("@graphql-tools/wrap");
const promise_helpers_1 = require("@whatwg-node/promise-helpers");
const parseAndPrintWithCache_js_1 = require("./parseAndPrintWithCache.js");
exports.MESH_API_CONTEXT_SYMBOL = Symbol('isMeshAPIContext');
function getInContextSDK(unifiedSchema, rawSources, logger, onDelegateHooks) {
    const inContextSDK = {};
    const sourceMap = unifiedSchema.extensions.sourceMap;
    for (const rawSource of rawSources) {
        const rawSourceLogger = logger?.child({ source: rawSource.name });
        const rawSourceContext = {
            rawSource,
            [exports.MESH_API_CONTEXT_SYMBOL]: true,
        };
        // TODO: Somehow rawSource reference got lost in somewhere
        let rawSourceSubSchemaConfig = rawSource;
        const stitchingInfo = unifiedSchema.extensions.stitchingInfo;
        if (stitchingInfo) {
            for (const [subschemaConfig, subschema] of stitchingInfo.subschemaMap) {
                if (subschemaConfig.name === rawSource.name) {
                    rawSourceSubSchemaConfig = subschema;
                    break;
                }
            }
        }
        // If there is a single source, there is no unifiedSchema
        const transformedSchema = sourceMap?.get(rawSource) || (0, delegate_1.applySchemaTransforms)(rawSource.schema, rawSource);
        const rootTypes = {
            query: transformedSchema.getQueryType(),
            mutation: transformedSchema.getMutationType(),
            subscription: transformedSchema.getSubscriptionType(),
        };
        rawSourceLogger?.debug(`Generating In Context SDK`);
        for (const operationType in rootTypes) {
            const rootType = rootTypes[operationType];
            if (rootType) {
                rawSourceContext[rootType.name] = {};
                const rootTypeFieldMap = rootType.getFields();
                for (const fieldName in rootTypeFieldMap) {
                    const rootTypeField = rootTypeFieldMap[fieldName];
                    const inContextSdkLogger = rawSourceLogger?.child({
                        inContextSdk: `${rootType.name}.${fieldName}`,
                    });
                    const namedReturnType = (0, graphql_1.getNamedType)(rootTypeField.type);
                    const returnsList = isListReturnType(rootTypeField.type);
                    const shouldHaveSelectionSet = !(0, graphql_1.isLeafType)(namedReturnType);
                    rawSourceContext[rootType.name][fieldName] = ({ root, args, context, info = {
                        fieldName,
                        fieldNodes: [],
                        returnType: namedReturnType,
                        parentType: rootType,
                        path: {
                            typename: rootType.name,
                            key: fieldName,
                            prev: undefined,
                        },
                        schema: transformedSchema,
                        fragments: {},
                        rootValue: root,
                        operation: {
                            kind: graphql_1.Kind.OPERATION_DEFINITION,
                            operation: operationType,
                            selectionSet: {
                                kind: graphql_1.Kind.SELECTION_SET,
                                selections: [],
                            },
                        },
                        variableValues: {},
                    }, selectionSet, key, argsFromKeys, valuesFromResults, autoSelectionSetWithDepth, dataLoaderOptions, }) => {
                        inContextSdkLogger?.debug(`Called with`, {
                            args,
                            key,
                        });
                        const commonDelegateOptions = {
                            schema: rawSourceSubSchemaConfig,
                            rootValue: root,
                            operation: operationType,
                            fieldName,
                            context,
                            transformedSchema,
                            info,
                            transforms: [],
                        };
                        // If there isn't an extraction of a value
                        if (typeof selectionSet !== 'function') {
                            commonDelegateOptions.returnType = rootTypeField.type;
                        }
                        if (shouldHaveSelectionSet) {
                            let selectionCount = 0;
                            for (const fieldNode of info.fieldNodes) {
                                if (fieldNode.selectionSet != null) {
                                    selectionCount += fieldNode.selectionSet.selections.length;
                                }
                            }
                            if (selectionCount === 0) {
                                if (!selectionSet) {
                                    if (autoSelectionSetWithDepth) {
                                        const operationNode = (0, utils_1.buildOperationNodeForField)({
                                            schema: transformedSchema,
                                            kind: operationType,
                                            depthLimit: autoSelectionSetWithDepth,
                                            field: fieldName,
                                        });
                                        selectionSet = (0, graphql_1.print)(operationNode.selectionSet.selections[0].selectionSet);
                                    }
                                    else {
                                        throw new Error(`You have to provide 'selectionSet' for context.${rawSource.name}.${rootType.name}.${fieldName}`);
                                    }
                                }
                                commonDelegateOptions.info = {
                                    ...info,
                                    fieldNodes: [
                                        {
                                            ...info.fieldNodes[0],
                                            selectionSet: {
                                                kind: graphql_1.Kind.SELECTION_SET,
                                                selections: [
                                                    {
                                                        kind: graphql_1.Kind.FIELD,
                                                        name: {
                                                            kind: graphql_1.Kind.NAME,
                                                            value: '__typename',
                                                        },
                                                    },
                                                ],
                                            },
                                        },
                                        ...info.fieldNodes.slice(1),
                                    ],
                                };
                            }
                        }
                        if (key && argsFromKeys) {
                            const batchDelegationOptions = {
                                ...commonDelegateOptions,
                                key,
                                argsFromKeys,
                                valuesFromResults,
                                dataLoaderOptions,
                            };
                            if (selectionSet) {
                                const selectionSetFactory = normalizeSelectionSetParamOrFactory(selectionSet);
                                const path = [fieldName];
                                const wrapQueryTransform = new wrap_1.WrapQuery(path, selectionSetFactory, identical);
                                batchDelegationOptions.transforms = [wrapQueryTransform];
                            }
                            const onDelegateHookDones = [];
                            const onDelegatePayload = {
                                ...batchDelegationOptions,
                                sourceName: rawSource.name,
                                typeName: rootType.name,
                                fieldName,
                            };
                            fixInfo(batchDelegationOptions.info, operationType);
                            return (0, promise_helpers_1.handleMaybePromise)(() => (0, promise_helpers_1.iterateAsync)(onDelegateHooks, onDelegateHook => onDelegateHook(onDelegatePayload), onDelegateHookDones), () => handleIterationResult(batch_delegate_1.batchDelegateToSchema, batchDelegationOptions, onDelegateHookDones));
                        }
                        else {
                            const regularDelegateOptions = {
                                ...commonDelegateOptions,
                                args,
                            };
                            if (selectionSet) {
                                const selectionSetFactory = normalizeSelectionSetParamOrFactory(selectionSet);
                                const path = [fieldName];
                                const wrapQueryTransform = new wrap_1.WrapQuery(path, selectionSetFactory, valuesFromResults || identical);
                                regularDelegateOptions.transforms = [wrapQueryTransform];
                            }
                            const onDelegateHookDones = [];
                            const onDelegatePayload = {
                                ...regularDelegateOptions,
                                sourceName: rawSource.name,
                                typeName: rootType.name,
                                fieldName,
                            };
                            fixInfo(regularDelegateOptions.info, operationType);
                            return (0, promise_helpers_1.handleMaybePromise)(() => (0, promise_helpers_1.iterateAsync)(onDelegateHooks, onDelegateHook => onDelegateHook(onDelegatePayload), onDelegateHookDones), () => handleIterationResult(delegate_1.delegateToSchema, regularDelegateOptions, onDelegateHookDones));
                        }
                    };
                }
            }
        }
        inContextSDK[rawSource.name] = rawSourceContext;
    }
    return inContextSDK;
}
function getSelectionSetFromDocumentNode(documentNode) {
    const operationDefinition = documentNode.definitions.find(definition => definition.kind === graphql_1.Kind.OPERATION_DEFINITION);
    if (!operationDefinition) {
        throw new Error('DocumentNode must contain an OperationDefinitionNode');
    }
    return operationDefinition.selectionSet;
}
function normalizeSelectionSetParam(selectionSetParam) {
    if (typeof selectionSetParam === 'string') {
        const documentNode = (0, parseAndPrintWithCache_js_1.parseWithCache)(selectionSetParam);
        return getSelectionSetFromDocumentNode(documentNode);
    }
    if ((0, utils_1.isDocumentNode)(selectionSetParam)) {
        return getSelectionSetFromDocumentNode(selectionSetParam);
    }
    return selectionSetParam;
}
const normalizeSelectionSetParamFactory = (0, utils_1.memoize1)(function normalizeSelectionSetParamFactory(selectionSetParamFactory) {
    const memoizedSelectionSetFactory = (0, utils_1.memoize1)(selectionSetParamFactory);
    return function selectionSetFactory(subtree) {
        const selectionSetParam = memoizedSelectionSetFactory(subtree);
        return normalizeSelectionSetParam(selectionSetParam);
    };
});
function normalizeSelectionSetParamOrFactory(selectionSetParamOrFactory) {
    if (typeof selectionSetParamOrFactory === 'function') {
        return normalizeSelectionSetParamFactory(selectionSetParamOrFactory);
    }
    return () => normalizeSelectionSetParam(selectionSetParamOrFactory);
}
function identical(val) {
    return val;
}
function handleIterationResult(delegateFn, delegateOptions, onDelegateHookDones) {
    return (0, promise_helpers_1.handleMaybePromise)(() => delegateFn(delegateOptions), delegationResult => (0, promise_helpers_1.handleMaybePromise)(() => (0, promise_helpers_1.iterateAsync)(onDelegateHookDones, onDelegateHookDone => onDelegateHookDone({
        result: delegationResult,
        setResult(newResult) {
            delegationResult = newResult;
        },
    })), () => delegationResult));
}
function fixInfo(info, operationType) {
    info.operation ||= {
        kind: graphql_1.Kind.OPERATION_DEFINITION,
        operation: operationType,
        selectionSet: {
            kind: graphql_1.Kind.SELECTION_SET,
            selections: [],
        },
    };
    info.operation.selectionSet ||= {
        kind: graphql_1.Kind.SELECTION_SET,
        selections: [],
    };
    info.operation.selectionSet.selections ||= [];
    info.operation.selectionSet.selections[0] ||= {
        kind: graphql_1.Kind.FIELD,
        name: {
            kind: graphql_1.Kind.NAME,
            value: '__typename',
        },
    };
    info.operation.selectionSet.selections[0].arguments ||= [];
}
function isListReturnType(type) {
    if ((0, graphql_1.isNonNullType)(type)) {
        type = type.ofType;
    }
    return (0, graphql_1.isListType)(type);
}
